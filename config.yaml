sample_table: "units_partial.tsv" 

# Prefix for out directory name
prefix: "EF_testing"

read_length: 150

# Genome and gtf file for STAR should not be gz. STAR also requires a GTF, not GFF file.

genome: "../dmel_ref/DmelRef.fasta" 

# For gatk, give full path to jar file
gatk.jar: "/home/jamie/GenomeAnalysisTK-3.2-2/GenomeAnalysisTK.jar"

# For picard, each function has it's own jar, provide folder containing them
picard.jar: "/home/jamie/picard-tools-1.79/picard-tools-1.79"
